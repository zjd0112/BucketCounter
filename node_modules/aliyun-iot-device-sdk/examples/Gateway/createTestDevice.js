const { signUtil } = require('../../');

module.exports = function createTestDevices() {
  return [
    {
      productKey: '<productKey>',
      deviceName: '<deviceName>',
      deviceSecret: '<deviceSecret>'
    }
  ].map(deviceConfig => {  
    return deviceConfig;
  });
};
